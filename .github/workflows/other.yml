name: CI

on: push

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: build
      uses: actions/setup-node@v2
      with:
        node-version: 15
    - run: npm ci
    - run: npm run build 

    - name: test 
    - run: npm test